# Setup

```bash
npm ci
npx playwright install chromium
npm run harness:check
npm run harness:test
npm run check:rules
npm run lint
npm test
npm run evidence:build
```

Zero tests is the expected initial state. Continue with
[From No Project to Test Metrics](../START-HERE.md); do not add application code before intake.
