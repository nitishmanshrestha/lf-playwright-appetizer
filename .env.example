# Populate only after project intake. This file contains names, never secrets.
BASE_URL=
TEST_ID_ATTRIBUTE=data-testid
PW_RETRIES=0

# CI Environment Flag
# Set to true in CI pipelines to run with 1 worker and 2 retries
# CI=true

# Add project-specific environment variable names after the application contract is approved.
